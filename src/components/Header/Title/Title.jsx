import React from 'react';
import classes from './Title.module.css';

const Title = () => {
  return (
    <div className={classes.container}>
      <span className={classes.text}>Menota</span>
    </div>
  );
}

export default Title;